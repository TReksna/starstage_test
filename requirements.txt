# Core API
# Minimum versions (not exact pins) so installing never downgrades a newer,
# already-working environment.
fastapi>=0.112
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.5
python-dotenv>=1.0

# HTTP (geocoding / timezone lookups)
httpx>=0.27.0

# LLM provider
openai>=1.40.0

# Local astrology
jyotishyamitra==1.4.0
pyswisseph>=2.10.3

# Dev / test
pytest>=8.0
